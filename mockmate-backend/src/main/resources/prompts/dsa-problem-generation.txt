Generate a DSA coding problem for a {difficulty} level interview at {company}.
Return ONLY valid compact JSON (no markdown, no extra text):
{
  "title": "short title",
  "description": "clear problem statement, 2-3 sentences",
  "constraints": ["constraint1", "constraint2"],
  "examples": [
    {
      "input": "human readable input",
      "output": "human readable output",
      "explanation": "why this output"
    }
  ],
  "testCases": [
    {"input": "exact stdin value", "expectedOutput": "exact stdout value"},
    {"input": "exact stdin value", "expectedOutput": "exact stdout value"},
    {"input": "exact stdin value", "expectedOutput": "exact stdout value"}
  ],
  "hints": [
    {"level": 1, "hint": "approach direction only"},
    {"level": 2, "hint": "data structure suggestion"},
    {"level": 3, "hint": "pseudocode outline"}
  ],
  "difficulty": "{difficulty}",
  "timeComplexityExpected": "O(...)",
  "spaceComplexityExpected": "O(...)",
  "inputFormat": "one of: int_array, int, string, int_array+int, string+int, int_array+int_array, string_array, string_array+int, matrix, matrix+int, binary_tree, linked_list",
  "outputFormat": "one of: int, string, boolean, int_array, string_array, binary_tree, linked_list",
  "methodSignature": "methodName only, no params, no types"
}

CRITICAL RULES FOR testCases:
- input field must be EXACTLY what stdin receives
- For int_array: space-separated integers on one line, example: "2 7 11 15"
- For int_array+int: array on line 1, integer on line 2, example: "2 7 11 15\n9"
- For string: just the string, example: "hello"
- For string+int: string on line 1, integer on line 2, example: "hello\n3"
- For matrix: first line = rows cols, then each row, example: "3 3\n1 2 3\n4 5 6\n7 8 9"
- For binary_tree: space-separated level-order values, null for missing, example: "4 2 7 1 3 6 9"
- For linked_list: space-separated node values, example: "1 2 3 4 5"
- expectedOutput must be EXACTLY what stdout should print
- For int_array output: space-separated (NOT comma-separated), example: "0 1" not "[0, 1]"
- For boolean: lowercase "true" or "false"
- Generate exactly 3 test cases
- Keep all strings under 50 characters
- No unicode, no special characters in test cases
- CRITICAL: inputFormat must list ALL types for the function parameters, lowercase, separated by +.
  - Example for String s and int k: "string+int"
  - Example for int[] nums and int target: "int_array+int"
  - The number of types in inputFormat MUST match the problem description.

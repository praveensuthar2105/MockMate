Generate a DSA coding problem for a {difficulty} level interview at {company}.
Return ONLY valid compact JSON (no markdown, no extra text):
{
  "title": "short title",
  "description": "clear problem statement, 2-3 sentences",
  "constraints": ["constraint1", "constraint2"],
  "examples": [
    {
      "input": "human readable description of input (e.g., 'nums = [1, 2, 3], target = 5')",
      "output": "human readable description of expected output (e.g., '2')",
      "explanation": "why this output is correct"
    }
  ],
  "testCases": [
    {"input": "exact stdin stream value (e.g., '3 5\\n1 2 3')", "expectedOutput": "exact stdout stream value (e.g., '2')"},
    {"input": "exact stdin stream value", "expectedOutput": "exact stdout stream value"},
    {"input": "exact stdin stream value", "expectedOutput": "exact stdout stream value"}
  ],
  "hints": [
    {"level": 1, "hint": "approach direction only"},
    {"level": 2, "hint": "data structure suggestion"},
    {"level": 3, "hint": "pseudocode outline"}
  ],
  "difficulty": "{difficulty}",
  "timeComplexityExpected": "O(...)",
  "spaceComplexityExpected": "O(...)",
  "inputFormat": "Clear description of stdin format (e.g. 'First line contains N and K. Second line contains N space-separated integers.')",
  "outputFormat": "Clear description of stdout format (e.g. 'Print a single integer representing the answer.')"
}

CRITICAL RULES FOR STDIN / STDOUT STREAMS:
1. The user writes a complete runnable program reading from stdin (e.g., using Scanner/BufferedReader) and printing to stdout.
2. `testCases.input` MUST NOT contain LeetCode parameter syntax like `nums = [1,2,1,2,3]`, `k = 2`, brackets, or variable declarations. It must contain ONLY the raw text stream that matches the `inputFormat` exactly.
3. Every testCase input MUST use newlines (`\n` or actual newline characters) or spaces to separate inputs exactly as a user scanner would expect.
   For example, if inputFormat says:
   - First line: integer N (array size).
   - Second line: N space-separated integers.
   - Third line: integer K.
   Then `testCases.input` MUST look exactly like:
   "5\n1 2 1 2 3\n2" (or using actual newline characters in the JSON).
4. `testCases.expectedOutput` must be the exact raw output (e.g., "3").
5. Keep all strings under 100 characters. No unicode or special characters.
6. Generate exactly 10 test cases. The first 3 test cases must correspond directly to the examples. The remaining 7 test cases must be diverse edge cases, extreme values, empty bounds, single-element cases, or large inputs to thoroughly validate correctness.
7. Keep all test case strings under 200 characters to prevent token bloat.



